{
  "name": "@home-dashboard/backend",
  "version": "7.1.1",
  "scripts": {
    "build": "rm -rf dist && tsc -b && cp package.json dist/package.json && cd dist && npm i --omit=dev",
    "deploy": "sls deploy --aws-profile tom",
    "lint-fix": "iamtomhewitt-linter --sourceFolder=./ --type=typescript --fix"
  },
  "dependencies": {
    "@iamtomhewitt/http": "2.2.0",
    "googleapis": "171.4.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "3.995.0",
    "@aws-sdk/client-ssm": "3.995.0",
    "@iamtomhewitt/linter": "2.2.0",
    "@types/aws-lambda": "8.10.122",
    "@types/node": "25.2.3",
    "aws-lambda": "1.0.7"
  }
}